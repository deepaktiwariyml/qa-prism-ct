// @qa-prism/ticketing — Jira + Linear adapters and remediation tracking (Phase 8)
// Phase 0 placeholder. Implemented in a later build phase (see spec §10).
export const PACKAGE = '@qa-prism/ticketing' as const;
