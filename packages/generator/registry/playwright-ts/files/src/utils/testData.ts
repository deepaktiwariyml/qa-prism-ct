/**
 * Centralised test data. Keep fixtures and expected values here rather
 * than hardcoding them inside tests — makes data changes a one-line edit.
 */
export const testData = {
  search: {
    query: 'assertions',
  },
  apiUser: {
    name: 'morpheus',
    job: 'leader',
  },
};
