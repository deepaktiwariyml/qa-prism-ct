// @qa-prism/generator — framework generator; port of prototype in /generator (Phase 6)
// Phase 0 placeholder. Implemented in a later build phase (see spec §10).
export const PACKAGE = '@qa-prism/generator' as const;
