// @qa-prism/scoring — aggregation + cross-pillar correlation engine (Phase 2)
// Phase 0 placeholder. Implemented in a later build phase (see spec §10).
export const PACKAGE = '@qa-prism/scoring' as const;
